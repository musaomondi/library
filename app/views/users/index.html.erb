<% provide(:title, 'All users') %>
<h1>All users</h1>
<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <% @users.each do |user| %>
        <tr>
            <td><%= user.name %></td>
            <td><%= user.email %></td></td></th>
        </tr>
    <% end %>
    </thead>
</table>
